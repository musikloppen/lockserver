version: '3.3'

services:
  perl_base:
    build:
      context: .
      dockerfile: ./Dockerfile.base
    image: perl-base:latest
    entrypoint: ["echo", "Base image build complete."]

  setup_config:
    image: perl-base:latest
    container_name: setup_config
    volumes:
      - ./db/conf.d:/out
    command: >
      sh -c "
        mkdir -p /out &&
        echo '[client]' > /out/99-client.cnf &&
        echo 'user=${DB_USER}' >> /out/99-client.cnf &&
        echo 'password=${DB_PASS}' >> /out/99-client.cnf &&
        echo 'host=${DB_HOST}' >> /out/99-client.cnf &&
        echo 'database=${DB_NAME}' >> /out/99-client.cnf &&
        echo 'port=${DB_PORT}' >> /out/99-client.cnf
      "
    environment:
      DB_NAME: ${DB_NAME}
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASS}

  db:
    build:
      context: .
      dockerfile: ./Dockerfile.db
    container_name: lock_db
    hostname: lock-db
    restart: always
    ports:
      - "3306:3306"
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASS}
      MARIADB_DATABASE: ${DB_NAME}
      MARIADB_USER: ${DB_USER}
      MARIADB_PASSWORD: ${DB_PASS}
      TZ: Europe/Copenhagen
    volumes:
      - ./db/data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u root -p\"$$MARIADB_ROOT_PASSWORD\" || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging:
      driver: "syslog"
      options:
        tag: "docker/db"

  redis:
    build:
      context: .
      dockerfile: ./Dockerfile.redis
    container_name: lock_redis
    hostname: lock-redis
    restart: always
    logging:
      driver: "syslog"
      options:
        tag: "docker/redis"

  unlock_server:
    build:
      context: .
      dockerfile: ./Dockerfile.unlock_server
    container_name: lock_unlock_server
    hostname: lock-unlock-server
    restart: always
    depends_on:
      - perl_base
      - redis
      - db
      - setup_config
    volumes:
      - ./db/conf.d:/etc/mysql/conf.d:ro
    environment:
      - DEBUG=${DEBUG}
      - REDIS_HOST=lock-redis
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - TZ=Europe/Copenhagen
    devices:
      - "/dev/gpiomem:/dev/gpiomem"
      - "/dev/mem:/dev/mem"
    privileged: true
    logging:
      driver: "syslog"
      options:
        tag: "docker/unlock_server"

  rfid_server:
    build:
      context: .
      dockerfile: ./Dockerfile.rfid_server
    container_name: lock_rfid_server
    hostname: lock-rfid-server
    restart: always
    depends_on:
      - perl_base
      - redis
      - unlock_server
      - db
      - setup_config
    volumes:
      - ./db/conf.d:/etc/mysql/conf.d:ro
    environment:
      - DEBUG=${DEBUG}
      - REDIS_HOST=lock-redis
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - TZ=Europe/Copenhagen
    privileged: true
    logging:
      driver: "syslog"
      options:
        tag: "docker/rfid_server"

  button_server:
    build:
      context: .
      dockerfile: ./Dockerfile.button_server
    container_name: lock_button_server
    hostname: lock-button-server
    restart: always
    depends_on:
      - perl_base
      - redis
      - unlock_server
      - db
      - setup_config
    volumes:
      - ./db/conf.d:/etc/mysql/conf.d:ro
    environment:
      - DEBUG=${DEBUG}
      - REDIS_HOST=lock-redis
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - TZ=Europe/Copenhagen
    devices:
      - "/dev/gpiomem:/dev/gpiomem"
      - "/dev/mem:/dev/mem"
    privileged: true
    logging:
      driver: "syslog"
      options:
        tag: "docker/button_server"

  web:
    build:
      context: .
      dockerfile: ./Dockerfile.web
    container_name: lock_web
    hostname: lock-web
    restart: always
    depends_on:
      - perl_base
      - redis
      - unlock_server
      - db
      - setup_config
    volumes:
      - ./db/conf.d:/etc/mysql/conf.d:ro
    extra_hosts:
      - "postfix:10.8.0.66"
    environment:
      - BASE_URL=${BASE_URL}
      - REDIS_HOST=lock-redis
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - NOTIFICATION_SMS_CODE_MESSAGE=${NOTIFICATION_SMS_CODE_MESSAGE}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASSWORD=${SMTP_PASSWORD}
      - SMTP_USE_TLS=${SMTP_USE_TLS}
      - DEBUG=${DEBUG}
      - TZ=Europe/Copenhagen
    logging:
      driver: "syslog"
      options:
        tag: "docker/web"

  openresty:
    build:
      context: .
      dockerfile: ./Dockerfile.openresty
    container_name: lock_openresty
    hostname: lock-openresty
    ports:
      - "${HTTP_PORT:-80}:80"
    depends_on:
      - perl_base
      - redis
      - unlock_server
      - db
      - setup_config
      - web
    volumes:
      - ./openresty/lua/dnsbl_whitelist:/usr/local/openresty/lualib/dnsbl_whitelist
    restart: always
    environment:
      - TZ=Europe/Copenhagen
    logging:
      driver: "syslog"
      options:
        tag: "docker/openresty"

  cron:
    build:
      context: .
      dockerfile: ./Dockerfile.cron
    container_name: lock_cron
    hostname: lock-cron
    restart: always
    depends_on:
      - perl_base
      - db
      - setup_config
    volumes:
      - ./db/conf.d:/etc/mysql/conf.d:ro
    environment:
      - DEBUG=${DEBUG}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - TZ=Europe/Copenhagen
    logging:
      driver: "syslog"
      options:
        tag: "docker/cron"
